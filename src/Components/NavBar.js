import React from "react";
function NavBar(){
    return (
        <header className="nav">
            <a href="#">HOME</a>
            <a href="#">ABOUT</a>
            <a href="#">GALLERY</a>
        </header>

    );
}
export default NavBar